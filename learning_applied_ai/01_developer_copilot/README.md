This code does have few issues i.e

Too long of response for small code, 
Does not stream the messages
It also replies to messages with out code in it and uses own code base to look into (it is a major issue people could read our own code base then)

Solution - 
Improve prompt
Add guardrails
stream messages

tbh it's a learning step so not required right now

also ugly print statements